---
phase: background-animation-redesign
task: choose-and-merge
status: awaiting_user_decision
last_updated: 2026-05-02T07:44:45.991Z
---

<current_state>
Two alternative background animations have been fully implemented, tested, and are running on local dev servers. The user needs to view both, pick one, and merge it to main.

- **Ribbon Waveform** → http://localhost:5174 (branch: bg/ribbon-waveform, dir: ../TALKYS-RIBBON)
- **Aurora Gradient** → http://localhost:5175 (branch: bg/aurora-gradient, dir: ../TALKYS-AURORA)

If the dev servers stopped, restart them:
```bash
cd ~/Documents/TALKYS-RIBBON && node node_modules/vite/bin/vite.js --port 5174
cd ~/Documents/TALKYS-AURORA && node node_modules/vite/bin/vite.js --port 5175
```
</current_state>

<completed_work>

**This session (main branch):**
- Diagnosed why the 3D animation (existing globe→wave) wasn't visible: every section had `bg-background` (solid, opaque) covering the fixed canvas
- Removed `bg-background` from all 8 section elements: HeroSection, ProblemSection, SolutionSection, HowItWorksSection, FeaturesSection, SocialMediaSection, IndustriesSection, GettingStartedSection
- Removed `futuristic-grid` CSS pattern from HeroSection (it was overlaying the 3D canvas with a competing grid pattern)
- Increased animation opacity: dark mode 0.25→0.6, light mode 0.15→0.45
- Brainstormed new animation directions via visual companion (browser at localhost:63891)
- Researched competitor AI voice sites (Bland, Retell, Vapi, Hume, ElevenLabs, Synthflow)
- Showed 4 advanced animated concepts (particles, spectrogram terrain, holographic head, Chladni)
- User chose Chladni but then pivoted to competitor-style inspiration
- Showed 4 competitor-style animations (ribbon waveform, aurora, particle constellation, liquid orb)
- User selected **ribbon waveform** and **aurora gradient** for parallel implementation

**Worktree: bg/ribbon-waveform (../TALKYS-RIBBON)**
- Created `src/utils/ribbonUtils.ts` — layer configs, buildRibbonPointsInto, buildSpinePointsInto, computeRibbonScroll
- Created `src/utils/ribbonUtils.test.ts` — 11 tests, all passing
- Replaced `src/components/BackgroundCanvas.tsx` with 6-layer ribbon waveform (R3F Line objects, DynamicDrawUsage, scroll-driven amplitude)
- 39 total tests passing, clean build

**Worktree: bg/aurora-gradient (../TALKYS-AURORA)**
- Created `src/utils/auroraUtils.ts` — BLOB_CONFIGS (5 blobs), hexToVec3, lerpColor
- Created `src/utils/auroraUtils.test.ts` — 8 tests, all passing
- Replaced `src/components/BackgroundCanvas.tsx` with full GLSL ShaderMaterial aurora (vertex + fragment shaders, Lissajous drift, film grain, scroll wakeup)
- 36 total tests passing, clean build

**Uncommitted on main (section bg-background removals + opacity increase):**
- src/sections/HeroSection.tsx — removed bg-background + futuristic-grid
- src/sections/ProblemSection.tsx — removed bg-background
- src/sections/SolutionSection.tsx — removed bg-background
- src/sections/HowItWorksSection.tsx — removed bg-background
- src/sections/FeaturesSection.tsx — removed bg-background
- src/sections/SocialMediaSection.tsx — removed bg-background
- src/sections/IndustriesSection.tsx — removed bg-background
- src/sections/GettingStartedSection.tsx — removed bg-background
- src/utils/morphUtils.ts — opacity increased (0.6 dark / 0.45 light)
</completed_work>

<remaining_work>

1. **User decides**: view http://localhost:5174 (ribbon) and http://localhost:5175 (aurora), pick one
2. **Merge chosen branch to main**: 
   - Commit the section changes on main first (the bg-background removals are unstaged)
   - Then merge the chosen worktree branch
3. **Clean up**: delete the unchosen worktree and branch
4. **Optional visual tuning**: after merge, adjust colours/opacity/speed to taste
</remaining_work>

<decisions_made>

- Removed `bg-background` from all sections — body already sets bg-background, sections don't need to repeat it; this lets the fixed canvas show through
- Removed `futuristic-grid` from hero — it competed visually with the 3D canvas
- Chose **Option B (WebGL fragment shader)** for aurora over CSS — richer visuals, GPU-driven, no layout reflow
- Ribbon uses R3F `line` + `bufferAttribute` with DynamicDrawUsage (same pattern as existing globe code) — consistent with codebase conventions
- Both implementations keep the same `BackgroundCanvas` component API — drop-in replacement, no changes needed elsewhere
- Used separate worktrees (not branches in same dir) so both can run simultaneously for comparison
</decisions_made>

<blockers>
- None technical. Awaiting user decision on which animation to keep.
</blockers>

<context>
The user is building the TALKYS website — an AI voice agent for businesses that answers calls and takes orders. The 3D background animation is meant to convey "AI avatar speaking." We went through several rounds of brainstorming (Chladni patterns, competitor research) before landing on ribbon waveform and aurora as the two finalists.

The existing animation (globe→wave morph on scroll, morphUtils.ts) is still on main and in the section-fixed worktrees. Both new animations are complete replacements of BackgroundCanvas.tsx only — everything else is untouched.

Node.js v23 breaks `.bin` shims in copied node_modules — use `node node_modules/vite/bin/vite.js` directly to start dev servers in the worktrees.

The visual companion server (brainstorming) was running at localhost:63891 — session files are at `.superpowers/brainstorm/`. Not needed for resume.
</context>

<next_action>
1. View both dev servers: http://localhost:5174 (ribbon) and http://localhost:5175 (aurora)
2. Tell Claude which one you prefer
3. Claude will: commit the section changes on main, merge chosen branch, delete the other worktree
</next_action>
