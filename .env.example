# Web3Forms access key for the "Book a Free Demo" form.
# 1. Go to https://web3forms.com
# 2. Enter alialfakih01@gmail.com (the primary recipient) and click "Create Access Key".
# 3. Paste the key below in a real .env.local file (not committed).
# 4. Restart `npm run dev` after editing .env.local — Vite only loads env at startup.
#
# CC list (ali.fakih@rentallsoftware.com, ali.alfakih@ssupworld.com) is wired in code
# and does not need an env var.
VITE_WEB3FORMS_KEY=
